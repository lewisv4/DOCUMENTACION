CREATE TABLE EJEMPLO (
    DNI VARCHAR(10),
    NOMBRE VARCHAR(10),
    APELLIDO VARCHAR(10),
    EDAD INT(10)
);

CREATE TABLE PRUEBA(
    ID INT(20)UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
    DNI INT(10)UNSIGNED NOT NULL,
    NOMBRE VARCHAR(10)NOT NULL,
    NOMBRE2 VARCHAR(10)NOT NULL,
    APELLIDO VARCHAR(10)NOT NULL,
    APELLIDO2 VARCHAR(10)NOT NULL,
    EDAD INT(4)UNSIGNED NOT NULL,
    FECHA_NACIMIENTO DATE NOT NULL
);

ALTER TABLE EJEMPLO ADD PRIMARY KEY (DNI);

CREATE  INDEX NOMBRE_INDICE ON EJEMPLO(NOMBRE);

CREATE UNIQUE INDEX NOMBRE_INDICE ON EJEMPLO(APELLIDO);

CREATE UNIQUE INDEX MIINDICE
ON EJEMPLO(NOMBRE,APELLIDO);